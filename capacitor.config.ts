import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.darayiban.mobile",
  appName: "دارایی‌بان",
  webDir: "out",
  android: {
    allowMixedContent: false,
  },
};

export default config;

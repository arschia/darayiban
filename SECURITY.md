# سیاست امنیتی دارایی‌بان

دارایی‌بان با داده‌های مالی شخصی، متن پیامک بانکی و توکن اتصال اتوماسیون iPhone/Android سروکار دارد. لطفاً مشکلات امنیتی را عمومی منتشر نکنید.

## گزارش آسیب‌پذیری

برای گزارش یک آسیب‌پذیری از بخش **Security → Report a vulnerability** همین مخزن استفاده کنید. در گزارش، مراحل بازتولید، اثر احتمالی و نسخه یا commit درگیر را بنویسید. برای آسیب‌پذیری امنیتی Issue عمومی نسازید.

## اطلاعاتی که نباید ثبت شوند

- کلید `SUPABASE_SERVICE_ROLE_KEY`
- فایل‌های `.env` یا `.env.local`
- توکن خام اتوماسیون کاربران
- فایل امضای Android (`.jks` یا `.keystore`) و رمزهای آن
- متن واقعی پیامک بانکی، شماره کامل کارت یا اطلاعات هویتی
- کلید OAuth یا اطلاعات ورود حساب‌ها
- کلید خصوصی VAPID و secret مربوط به Cron

نمونه‌ها و اسکرین‌شات‌ها باید پیش از ارسال ناشناس‌سازی شوند.

## اصول پیاده‌سازی

- داده هر کاربر با Row Level Security از دیگر کاربران جدا می‌شود.
- عملیات دارای دسترسی مدیریتی فقط در Supabase Edge Function انجام می‌شود.
- کلیدهای سمت سرور هیچ‌گاه با پیشوند `NEXT_PUBLIC_` منتشر نمی‌شوند.
- توکن اتوماسیون به‌صورت هش‌شده ذخیره و قابل لغو است.
- در Android توکن محلی با AES/GCM و کلیدی از Android Keystore رمزگذاری می‌شود.
- گیرنده پیامک پیش از ارسال، پیام‌های رمز پویا و کد ورود را فیلتر می‌کند؛ پردازش نهایی نیز در Edge Function انجام می‌شود.
- وابستگی‌ها، lint، TypeScript و build در CI بررسی می‌شوند.
- کلیدهای Web Push داخل Supabase Vault نگهداری می‌شوند و فقط RPC محدودشده به `service_role` به آن‌ها دسترسی دارد.

## محدوده پشتیبانی

آخرین نسخه شاخه `main` پشتیبانی می‌شود. اصلاح امنیتی پس از بررسی، ابتدا به‌صورت خصوصی آماده و سپس همراه با توضیح حداقلی منتشر می‌شود.
